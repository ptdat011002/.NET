using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;
using PhamTriDat.Solution;

namespace PhamTriDat
{
    static class Program
    {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            // Application.Run(new baitap1());
            // Application.Run(new baitap2());
            // Application.Run(new baitap3());
            // Application.Run(new baitap4());
            // Application.Run(new baitap5());
             Application.Run(new baitap6());
        }
    }
}
